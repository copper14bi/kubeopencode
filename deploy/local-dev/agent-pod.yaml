# Copyright Contributors to the KubeOpenCode project
# Pod-mode Agent for local testing
#
# This Agent creates a new Pod for each Task (traditional mode).
# Each Task runs in isolation with its own container.
#
# Features demonstrated:
#   - templateRef: inherits shared config from AgentTemplate
#   - Pod mode: ephemeral, one Pod per Task
#
apiVersion: kubeopencode.io/v1alpha1
kind: Agent
metadata:
  name: pod-agent
spec:
  # Inherit shared configuration from AgentTemplate
  templateRef:
    name: local-dev-base
  profile: "Ephemeral pod-mode agent for isolated task execution"
  # Required fields (also in template, but required on Agent)
  executorImage: quay.io/kubeopencode/kubeopencode-agent-devbox:latest
  workspaceDir: /workspace
  serviceAccountName: kubeopencode-agent
  # Mount OpenCode API credentials (overrides template to demonstrate override)
  credentials:
    - name: opencode
      secretRef:
        name: opencode-credentials
